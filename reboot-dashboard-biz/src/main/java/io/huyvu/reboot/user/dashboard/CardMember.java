package io.huyvu.reboot.user.dashboard;

public record CardMember(long id,
                         String username,
                         String fullName,
                         String pictureUrl) {
}
